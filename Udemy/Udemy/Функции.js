let b = 50;

 function sum (a) {
    let num = b + a;
    return num;
 }

 console.log(sum(20));





const usdCurr = 28;

function convert (amount, curr) {
    console.log(curr * amount);
}

convert(500, usdCurr);